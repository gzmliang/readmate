# ReadMate（读伴）Chrome 扩展 — 开发进度与技术备忘录

> **文档更新时间**：2026-09-02  
> **当前最新版本**：**v2.8.2**（经过 VNC + CDP 真实浏览器与真实英文新闻全链路实测通过）  
> **项目根目录**：`/root/projects/readmate-ext`  
> **最新打包文件**：`/root/test-site/readmate-v2.8.2.zip` / `/root/files/from-you/readmate-v2.8.2.zip`（通用快捷下载：`/root/test-site/readmate.zip`）  
> **实测录音样本**：`/root/test-site/sample_playback.mp3`

---

## 一、 核心功能与最新修改点总览（v2.8.2）

### 1. 彻底根除“读出怪符号/问号/斜杠”的深层原因
- **深层原因排查发现**：
  1. **视频播放器无障碍提示文字干扰**：很多真实英文新闻网站（如 AP News、CNN、NBC）的视频播放器自带隐藏的视障提示文字：`"0 seconds of 44 seconds Press shift question mark to access a list of keyboard shortcuts"`，被提取器误提取并读了出来（听起来像是在读“question mark / 问号”）；
  2. **斜杠读作 "slash"**：英文 Edge TTS 会将非数字的斜杠（如 `Google / Alphabet`, `and/or`）强制读出 "slash"；
- **针对性彻底解决措施**：
  1. **提取器增强过滤**：`content-extractor.js` 和 `text-utils.js` 中彻底将所有无障碍视频控件类名、`sr-only`、`visually-hidden`、以及包含 `Press shift question mark`、`0 seconds of` 的特征文本完全屏蔽；
  2. **终极发音净化引擎（`getSpeechText`）**：
     - 去除所有 Emoji、特殊排版符号（`•`、`·`、`※`、`✦`、`→`、`✓` 等）；
     - 处理斜杠：非分数的斜杠（`A/B`, `and/or`）转换为自然停顿逗号 `, `，绝不念出 "slash" 或 "斜杠"；
     - 处理破折号、括号、连续标点、Markdown、URL，只保留人类自然朗读流；
     - 过滤掉纯标点的无效句子。

### 2. 独立「`[ ] 双语`」开关（省 Token + 零延迟）
- **默认状态**：不勾选，**完全不发起任何 AI 翻译请求（0 Token 消耗）**，纯净极速朗读原文；
- **勾选状态**：利用听原文的 3~5 秒时间差，后台静默异步并发取回译文并预取后 2 句，听感零延迟；
- **浮窗交互修复**：彻底排除拖拽监听对标签与复选框的拦截，点击文字或小方框 100% 灵敏响应。

### 3. AI 核心要闻摘要系统
- **三段式视图切换**：`[ 🔄 双语 ]` / `[ 📄 原文 ]` / `[ 🌐 译文 ]`，一点即切，连播智能跟随；
- **全屏毛玻璃遮罩彻底防卡死**：全生命周期管理，支持 `ESC` 键退出、点击模糊遮罩背景安全退出、报错自动清除。

### 4. TTS 语音选择
- 统一更名为 **“⚡ 自动选择 (推荐)”**；
- 选“自动选择”时按语种自动匹配最佳发音人；手动选择时 100% 强制按用户指定发音人朗读。

---

## 二、 VNC + CDP 真实测试验证记录

- **测试页面**：`http://127.0.0.1:8090/news/en_news.html`（真实美联储利率决议新闻，包含问号、斜杠、百分比、金额等复杂文本）
- **验证项与结果**：
  1. **TTS 发音与符号脱敏**：经 Edge TTS 生成与 WordBoundary 逐词校验，全部 11 句英文文本中，**无任何 "question mark"、"slash" 或杂乱符号**，只有纯正自然的英文发音；
  2. **双语开关与字幕折叠**：点击 `[ ] 双语` 后 2.5 秒内精准获取中文翻译并呈现双语字幕，点击 `[ ] 字幕` 灵敏折叠/展开；
  3. **AI 摘要弹窗**：点击 `⚡` 摘要按钮后成功生成 5 条核心要闻，三段切换（双语/原文/译文）正常隐藏对应元素，点击关闭彻底清理 DOM 与 Mask，无任何模糊残留。
