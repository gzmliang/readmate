# 读伴 ReadMate 版本变更记录（CHANGELOG）

> 本文件是读伴对外版本号的**唯一权威说明**。任何交付、提审、网站介绍、文档一律以本文件 + `manifest.json` 为准。

---

## 一、版本号规则（梁老师 2026-09-26 拍板）

1. **单一来源**：商店/对外版本号只认 `manifest.json` 里的 `version`（同时同步写 `version_name`），别处不许再自造版本号。
2. **逐步递进**：公开发布线按 `1.0.x` 一格一格往上走，**以 Chrome 商店已上架版本为基准 +1**，绝不跳跃、绝不倒退。
3. **发布四件套（缺一不可）**：
   - 改 `manifest.json` 的 `version` / `version_name`；
   - 在本文件追加一条变更记录；
   - `git commit` 后**打同名 tag**（如 `v1.0.9`）并 `git push origin main --tags`；
   - 重新打包并用新版本号包名上传（个人网站 + 内部交付三通道）。
4. **tag 不可覆盖**：已存在的 tag 一律不动（含早期历史 tag），新版本只能新增 tag。
5. **一个版本对应一个包**：包名带版本号（`ReadMate-Extension-v1.0.9.zip`）；内部调试期沿用锁死的固定包名 `readmate-reader-m1-store.zip`，发布时以正式版本号另存。

---

## 二、历史版本线说明（重要：仓库里曾经有三条线，别再混）

| 版本线 | 版本号范围 | 时间 | 状态 |
|---|---|---|---|
| 早期实验线 | `v1.0` → `v1.4.3` | 2026-06 ~ 2026-07 | 内部实验，已归档（tag 保留，不对外引用） |
| 内部开发线 | `v2.9.0` → `v2.14.1` | 2026-09-05 | 内部开发，未上架商店（tag 保留，不对外引用） |
| **商店公开线** | **`v1.0.0` → `v1.0.9`** | 2026-09 起 | ✅ **对外唯一有效版本线**（本文件下半部分详录） |

> ⚠️ 注意：6 月老线里已存在 `v1.1.1`、`v1.2.0`、`v1.4.x` 等同名 tag，与 2026-09-26 内部开发时口头用的 "1.1.0 / 1.1.1" **不是一回事**。
> 2026-09-26 内部开发版 `1.1.0 / 1.1.1`（点读气泡确认、i18n 全量清理）**从未上架商店**，其功能全部并入公开发布 **v1.0.9**，不再单独占号。

---

## 三、商店公开线变更记录

### v1.0.9 — 2026-09-26 【最新发布】
> 主题：**读伴能读整本书了** + 点读体验大修

**🌟 全新「读伴书页」（Reader / Book Page）**
- 支持导入本地书稿当书读：`txt` / `md` / `epub` / `PDF`
- **epub**：原生解包（不依赖第三方库），自动读取书内目录（EPUB3 nav / EPUB2 ncx 两套都认）、剔除脚本样式图片与脚注角标；注音绘本自动剔除拼音，不会把 `liè nà hú` 当正文念出来
- **PDF**：内置 pdf.js 引擎（随扩展打包，**绝不联网**，商店合规），逐页抽取文字层并重排为书页；断词连字符自动拼回、页眉页脚页码剔除；**扫描版与损坏文件明确报错**（人话提示，绝不假装成功）
- 书库管理、阅读进度自动记忆、6 套护眼主题（与沉浸净读同源）
- 一键导出 **PDF 打印稿** 与 **双语独立 HTML**（可脱离扩展打开）
- 远程导入：网页上的 `.epub` / `.pdf` 链接可右键直接送进书页

**🎯 点读体验重做**
- 单击正文默认改为**气泡确认后再开播**（根治「随手一点突然开口」的最高频投诉）；可在设置里切回「直接开播」
- **指哪读哪**：净读模式点句子、原网页点段落，都从**鼠标所在那一句**开播（不再是永远从第 1 句开始）
- 划词翻译结果改为**常驻卡片**（30 秒兜底、可选中复制、✕ 关闭、点别处关闭）

**🖱️ 悬浮球可自由拖动**（兼容 Android Kiwi 触屏）；收起态不再遮挡网页内容，隐藏竖条点击穿透

**🌐 界面文案 100% 多语言**：彻底清理全部中文兜底文案（169 处）与裸露词典键名，8 国语言词典键集完全对齐，并新增 `tools/i18n-check.js` 自动守卫（退出码非 0 即不合规）

**🖍️ 高亮零侵入**：划词/标题朗读不再把大标题改小字体，原网页排版零位移、零抖动

**🔒 安全**：全面 HTTPS 加密传输（延续 v1.0.8 合规整改）

**📌 补记（2026-09-26）商店文案整改**：v1.0.9 提审因商店「商品说明」被判 Yellow Argon（关键字垃圾内容）——被点名英文句 `Import txt, Markdown, EPUB and PDF files`。根因：新增的 What's New 段落把 `txt / Markdown / EPUB / PDF` 这串格式关键词重复堆了 9 次，且与下方功能章节重复叙述同一件事。整改：删除整个 What's New 块、删除开头人群词（language learners / researchers / daily readers）、文件格式枚举全篇清零，发布日志改由后台独立的 What's new 字段承载。整改后文案见 `store-assets/store-description.md`（**商店自此只用英文文案**，中文介绍仅保留在个人网站）。

---

### v1.0.8 — 2026-09-21
- 全面升级 HTTPS 加密传输，消除明文 HTTP 传输用户数据违规（商店 Purple Copper 警告），安全合规整改

### v1.0.7 — 2026-09-21
- 根治划词高亮把页面大标题误改小字体的历史缺陷（严格区分临时高亮节点与宿主原生 DOM，永不删除宿主节点）
- 原网页排版零侵入、零位移（段落指示线改用 `box-shadow: inset`）

### v1.0.6 — 2026-09-18
- 默认免费双轨翻译引擎（内置免 Key 极速接口）
- 双语研读排版：原文 / 双语对照 / 译文 三档一键切换
- 四合一 PDF 导出体系（纯原文 / 双语上下 / 左右双栏 / 纯译文）

### v1.0.3 — 2026-09-10
- 多语言智能感知升级（日/韩/德/法/西/意/俄），亚洲语种字素区分消除误判中文
- 欧洲语种变音符号与词汇启发式打分，发音引擎加固

### v1.0.0 — 2026-09 初
- 商店公开线起点：网页朗读、沉浸净读、划词查词、AI 摘要、全球自然人声

---

## 四、发布核对清单（每次发布照着走）

```bash
cd /root/projects/readmate-ext
grep '"version"' manifest.json                      # 1. 确认版本号已改
bash tools/pack-store.sh                            # 2. 打包 + 包内冒烟测试（11 项）
cd /root/projects/readmate-e2e-tests
node server.js & node test.js                       # 3. 老功能回归（86 项）
node reader-test.js                                 # 4. 书页 M1（92 项）
node reader-format-test.js                          # 5. 书页 M2/M3（75 项）
cd /root/projects/readmate-ext
git add -A && git commit -m "release(vX.Y.Z): ..."   # 6. 提交
git tag vX.Y.Z && git push origin main --tags        # 7. 打 tag 并推送
# 8. 上传个人网站 /downloads/ + 内部交付三通道，并核对 MD5 一致
```
