# ReadMate（读伴）v2.0 架构设计与开发交接文档

> **文档定位**：指导 ReadMate 从“新闻轮播”向“单篇深度多语种听读与 AI 学习助手”转型的核心规范与实施指南。
> **核心原则**：做减法砍掉不稳定功能，专注单篇极致体验，面向全球化多语种需求。

---

## 一、战略调整与功能裁剪（减法）

### 1. 彻底砍掉“轮播/多页跳转连播”
- **原因**：现代网站（SPA、瀑布流、反爬、跨域限制）结构过于复杂，通用自动跳页易错且维护成本高。
- **清理范围**：
  - 移除 `scanAndShowPlaylist`、`showPlaylistDrawer`、序号徽章、跨页跳转与 session 连播队列逻辑。
  - 右下角悬浮按钮（FAB）回归**单一极简圆形播放按钮**（轻量无干扰）。

---

## 二、核心新特性体系设计（加法）

### 1. 朗读语音流模式（用户自由三选一）
为满足不同用户场景（母语者纯听、外语学习者双语对照、普通读者直接听母语译文），提供三档播放模式：
1. **🔊 仅读原文（Original Only）**：适合听力无障碍或读母语内容，低延迟纯粹朗读。
2. **🌐 直接读译文（Translated Only）**：自动将原文译为目标语言，直接用目标语言音色朗读（外文新闻秒变母语电台）。
3. **🔄 双语交替读（Bilingual Alternating）**：读一句原文（原声音色）+ 读一句译文（译声音色），外语精读与听力神器。

### 2. 智能多语种与音色联动体系（Multi-language & TTS Auto-Mapping）
- **原文语言自动检测**：通过网页 meta/HTML lang 及文本特征自动识别（如 en, zh, ja, ko, fr, de, es 等）。
- **目标语言自由设定**：支持简体中文、英语、日语、韩语、法语、德语、西班牙语等全球主流语种。
- **音色智能映射（Edge TTS / 原生语音）**：
  - 读原文时自动匹配该语种高质量音色（如 en 匹配 `en-US-JennyNeural`，ja 匹配 `ja-JP-NanamiNeural`）。
  - 读译文时自动切换至目标语言音色（如 zh 匹配 `zh-CN-XiaoxiaoNeural`）。

### 3. 多语种 AI 智能摘要（AI Multilingual Summary）
- **触发入口**：浮动控制条上常驻 **⚡ 摘要** 按钮。
- **返回结构**：一次 AI 请求，返回 3 条核心要点，且每条包含 `原文摘要` 与 `译文摘要`。
- **播放与操作**：
  - 摘要卡片支持：**【🗣️ 读原文摘要】**、**【🌐 读译文摘要】**、**【🔄 双语对照连播】**。
  - 支持一键将摘要复制或导出为 Markdown。

### 4. 朗读控制条重构（双语动态歌词/字幕卡片）
- **外观风格**：悬浮毛玻璃胶囊卡片，支持拖拽与记忆位置。
- **双层布局**：
  - **顶部控制栏**：播放/暂停、语速微调（0.5x~2.5x）、模式切换下拉（原文/译文/双语）、⚡摘要、📥导出MP3、⏹停止、✕关闭。
  - **底部字幕区（带开关）**：
    - 带 `☑ 显示翻译字幕` 勾选项。
    - 开启时展开：
      - **行 1（原文）**：字号较大，当前读到的词句高亮呼吸光效。
      - **行 2（译文）**：实时显示对应翻译（支持预取下一句，保证音画零延迟）。
    - 关闭时折叠为极简单行模式。

### 5. 极简 AI 服务商接入（一键预设配置）
解决普通用户配置 API 的门槛，提供预设下拉菜单：
- **DeepSeek（推荐）**：自动填入 `https://api.deepseek.com/v1` 与 `deepseek-chat`，仅需填 Key。
- **OpenAI (ChatGPT)**：官方端点与 `gpt-4o-mini`。
- **SiliconFlow（硅基流动）**：国内高性价比聚合平台。
- **Moonshot (Kimi)** / **阿里云通义千问** / **零一万物**。
- **自定义（Custom）**：开放给高级用户自由配置兼容 OpenAI 格式的任意端点与模型。

### 6. 断点续听与记忆（Reading Progress Resume）
- 以页面 URL 为键，在本地轻量保存播放进度（朗读到的句子序号与比例）。
- 再次打开长文时，浮动条角落友好提示：`⏱ 上次读到 45%（第 12 句） [继续播放] [重新开始]`。

### 7. 导出 MP3 音频文件
- 利用云端 Edge TTS 服务端直接合成整篇/所选音频流，一键触发浏览器下载保存为 `.mp3` 文件。

### 8. 自定义文本导入朗读
- Popup 与右键菜单提供 **📝 文本朗读板**，支持粘贴外部文本或拖入 `.txt` 随时调用控制条朗读。

---

## 三、系统架构与技术实施要点

```
┌─────────────────────────────────────────────────────────────┐
│                    Content Extractor                        │
│             正文精准提取 / 智能语种识别 (LangDetect)          │
└──────────────────────────────┬──────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────┐
│                    Reading Controller                       │
│    模式控制 (仅原文 / 仅译文 / 双语交替) + 进度断点记忆      │
└──────────────┬───────────────────────────────┬──────────────┘
               │                               │
┌──────────────▼──────────────┐ ┌──────────────▼──────────────┐
│       AI Service API        │ │      Dual TTS Engine        │
│ 逐句预翻译 / 多语种3句摘要  │ │ 自动语种音色映射 (原文/译文)│
└─────────────────────────────┘ └─────────────────────────────┘
```

1. **翻译预取流水线（Prefetch Translation Queue）**：
   - 朗读第 $N$ 句时，后台异步发起第 $N+1$、$N+2$ 句的翻译请求并存入 LRU 缓存，彻底杜绝读译文或双语时的卡顿。
2. **高亮对齐容错**：
   - 读原文时：高亮原网页 DOM 句子。
   - 读译文时：高亮原网页对应原文段落，控制条大字显示当前译文。

---

## 四、开发实施里程碑推荐

| 阶段 | 核心任务 | 交付物 |
| :--- | :--- | :--- |
| **Phase 1（核心重构）** | 1. 彻底清除轮播旧代码，精简 FAB<br>2. 重构控制条（双层卡片+翻译字幕折叠+模式切换下拉）<br>3. 接入 AI 预设下拉配置 + 逐句翻译预取流水线<br>4. 实现【仅原文 / 仅译文 / 双语交替】三模式 | **v2.0-beta1** |
| **Phase 2（AI 深度赋能）**| 1. 多语种双核 3 句 AI 摘要（卡片展示 + 原文/译文/双语独立朗读）<br>2. 原文语种自动识别与 Edge TTS 音色智能映射 | **v2.0-beta2** |
| **Phase 3（实用扩展）** | 1. 页面断点续听记忆<br>2. 导出 MP3 文件<br>3. 自定义文本导入朗读板 | **v2.0 正式版** |
